# React Application Tempalte

how to make it work?

**In vscode**

Install `live server` extension.

Run the follow command:

```sh
npm i
npm run watch
```

Open the live server to browse the `dist/index.html` websites.


**Why I want to to reduce the package number?**

I think reducing the number of package can make the project simple.